<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.leezu.mapper.userMapper">
	
	<!-- 회원가입 -->
	<insert id="userReg" parameterType="DTO.UserReg">
	<!--  0은 authority(관리자 : 1) 1은 emailchecked(이메일인증 : 1) -->
	insert into test_user
		values( #{userID},
				#{userPassword},
				#{userName},
				#{userEmail},
				'0',
				'1')
	</insert>
	
	<!-- 로그인 -->
	<select id="userLogin" parameterType="DTO.UserDTO">
		select USERID, USERPASSWORD
			from test_user
		where	USERID = #{userID}
		and		USERPASSWORD = #{userPassword}
	</select>
	
	<!-- 아이디 검색 -->
	<select id="selectByID" resultType="DTO.UserDTO">
		select * from test_user
		where USERID = #{userID}
	</select>
	
	<!-- 회원 조회 -->
	<select id="userList" resultType="DTO.UserDTO">
		select *
		from test_user
		order by USERNAME
	</select>
</mapper>